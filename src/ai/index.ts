/**
 * AI module — future home of Google Gemini integration.
 *
 * Planned exports:
 * - GeminiClient        — wraps the Gemini API SDK
 * - ConversationManager — manages multi-turn dialogue state
 * - PromptBuilder        — constructs system/user prompts for receptionist tasks
 * - IntentClassifier     — classifies caller intent from transcript
 */

export {};
