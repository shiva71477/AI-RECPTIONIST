/**
 * API module — houses route handler logic (controllers).
 *
 * Each handler file corresponds to a domain:
 * - twilioHandler.ts   — processes Twilio webhook payloads
 * - calendarHandler.ts — processes calendar CRUD requests
 * - aiHandler.ts       — processes AI conversation requests
 * - crmHandler.ts      — processes CRM record requests
 */

export {};
