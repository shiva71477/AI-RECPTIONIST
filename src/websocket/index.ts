/**
 * WebSocket module — real-time bidirectional communication.
 *
 * Planned exports:
 * - MediaStreamServer  — handles Twilio Media Stream WebSocket connections
 * - AudioPipeline      — routes raw audio chunks → WhisperFlow → Gemini → TTS
 * - SessionManager     — tracks active call sessions by CallSid
 */

export {};
