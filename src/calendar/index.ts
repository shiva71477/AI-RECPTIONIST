/**
 * Calendar module — Google Calendar integration.
 *
 * Planned exports:
 * - CalendarClient      — wraps Google Calendar API (OAuth2)
 * - AvailabilityChecker — queries free/busy slots for a given calendar
 * - AppointmentService  — creates, updates, and cancels appointments
 * - TokenStore          — stores and refreshes OAuth2 tokens
 */

export {};
