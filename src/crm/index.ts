/**
 * CRM module — customer record management.
 *
 * Planned exports:
 * - ContactService  — lookup/upsert callers in Supabase
 * - CallLogService  — persist call records, transcripts, and outcomes
 * - LeadService     — create or update leads from call data
 */

export {};
